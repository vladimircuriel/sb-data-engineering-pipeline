name: sb_pipeline
version: '1.0.0'
config-version: 2

profile: sb_pipeline

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sb_pipeline:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: dwh
